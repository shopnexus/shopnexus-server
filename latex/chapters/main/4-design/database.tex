% ============================================================
% 4.3 Thiết kế cơ sở dữ liệu
% Owner: Common (Manager)
% ============================================================

\section{Thiết kế cơ sở dữ liệu}
\label{sec:database}

Module Order sử dụng schema \texttt{order.*} trên PostgreSQL~18, gồm 7 bảng và 3 kiểu enum, chuẩn hoá 3NF. Thiết kế tuân theo mô hình \textit{pay-first checkout}: buyer thanh toán ngay tại checkout, items được tạo với \texttt{payment\_id}. Trạng thái item được suy ra từ \texttt{payment.status}, \texttt{order\_id} và \texttt{date\_cancelled} thay vì lưu trực tiếp. Không có foreign key liên schema --- tính toàn vẹn liên module được đảm bảo ở tầng nghiệp vụ.

\begin{figure}[H]
\centering
\includegraphics[
  angle=90,
  height=22cm,
  keepaspectratio,
  clip
]{uploads/erd.png}
\caption{Lược đồ ERD của module Order}
\label{fig:order-erd}
\end{figure}

% ------------------------------------------------------------------
\subsection{Danh sách bảng}
\label{subsec:db-tables}

\small
\rowcolors{2}{white}{rowgray}
\begin{longtable}{@{}L{2.5cm}L{5.5cm}L{5.5cm}@{}}
\caption{Các bảng trong schema \texttt{order}}\label{tab:order-tables}\\
\toprule
\rowcolor{headerblue}
\thead{Bảng} & \thead{Cột chính} & \thead{Mục đích} \\
\midrule
\endfirsthead
\toprule
\rowcolor{headerblue}
\thead{Bảng} & \thead{Cột chính} & \thead{Mục đích} \\
\midrule
\endhead
\midrule
\multicolumn{3}{r}{\small\textit{Tiếp theo trang sau}} \\
\endfoot
\bottomrule
\endlastfoot

cart\_item & id, account\_id, sku\_id, quantity & Giỏ hàng tạm thời; unique (account\_id, sku\_id) \\
payment & id, account\_id, option, status, amount, data, date\_expired & Phiên thanh toán; data chứa JSONB provider-specific \\
transport & id, option, status, cost, data & Vận chuyển; status enum 7 trạng thái lifecycle \\
order & id, buyer\_id, seller\_id, transport\_id, product\_cost, product\_discount, transport\_cost, total, address & Đơn hàng; total = cost $-$ discount + transport \\
item & id, order\_id, payment\_id, account\_id, seller\_id, sku\_id, sku\_name, quantity, unit\_price, transport\_option, transport\_cost\_estimate, date\_cancelled, serial\_ids & Mục đơn hàng; trạng thái suy ra từ payment.status, order\_id, date\_cancelled \\
refund & id, account\_id, order\_id, confirmed\_by\_id, transport\_id, method, status, reason, address & Yêu cầu hoàn trả; method: PickUp/DropOff \\
refund\_dispute & id, refund\_id, issued\_by\_id, reason, status & Tranh chấp; issued\_by\_id là buyer hoặc seller \\
\end{longtable}
\rowcolors{1}{}{}
\normalsize

% ------------------------------------------------------------------
\subsection{Ràng buộc và chỉ mục}
\label{subsec:db-constraints}

\begin{itemize}[nosep]
    \item \textbf{Enum dùng chung:} \texttt{order.status} (Pending, Processing, Success, Cancelled, Failed) được dùng cho payment, order, refund, refund\_dispute.
    \item \textbf{Partial index:} \texttt{item\_pending\_seller\_idx} và \texttt{item\_pending\_account\_idx} lọc \texttt{WHERE order\_id IS NULL} --- tối ưu truy vấn inbox mục chờ.
    \item \textbf{FK cascade:} \texttt{item.order\_id} $\to$ \texttt{order.id} (ON DELETE CASCADE); \texttt{refund.order\_id} $\to$ \texttt{order.id} (ON DELETE NO ACTION --- giữ lại refund record).
    \item \textbf{FK set null:} \texttt{order.transport\_id} (ON DELETE SET NULL); \texttt{item.payment\_id} $\to$ \texttt{payment\allowbreak.id} (ON DELETE SET NULL).
    \item \textbf{Unique:} \texttt{cart\_item(account\_id, sku\_id)} --- mỗi tài khoản chỉ có 1 dòng/SKU trong giỏ.
\end{itemize}

% ------------------------------------------------------------------
\subsection{Truy vấn dữ liệu (SQLC queries thay cho Stored Procedure)}
\label{subsec:db-sqlc}

ShopNexus không dùng Stored Procedure truyền thống. Thay vào đó, toàn bộ truy vấn được quản lý bằng \textbf{SQLC} --- công cụ sinh mã nguồn type-safe trực tiếp từ SQL thuần. So với Stored Procedure:

\begin{itemize}[nosep]
    \item \textbf{Quản lý phiên bản}: các file SQL nằm cùng mã nguồn, được rà soát qua Pull Request như mọi thay đổi khác.
    \item \textbf{An toàn kiểu}: SQLC sinh struct tương ứng với schema, phát hiện lỗi tại thời điểm biên dịch thay vì lúc chạy.
    \item \textbf{Triển khai tự động}: truy vấn được sinh và triển khai cùng mã nguồn, không cần migration riêng.
    \item \textbf{Phân tách rõ ràng}: tầng truy vấn đóng vai trò Repository --- logic nghiệp vụ chỉ gọi hàm đã sinh, không viết SQL trực tiếp.
    \item \textbf{Đánh đổi}: mất cached execution plan và phân quyền ở tầng DB; bù lại bằng sự đơn giản và khả năng kiểm thử.
\end{itemize}

\small
\rowcolors{2}{white}{rowgray}
\begin{longtable}{@{}L{3.8cm}L{3.5cm}L{2.8cm}L{3.2cm}@{}}
\caption{Các query SQLC chính (tương đương Stored Procedure)}\label{tab:db-sqlc-queries}\\
\toprule
\rowcolor{headerblue}
\thead{Query} & \thead{Nhiệm vụ} & \thead{Inputs} & \thead{Outputs} \\
\midrule
\endfirsthead
\toprule
\rowcolor{headerblue}
\thead{Query} & \thead{Nhiệm vụ} & \thead{Inputs} & \thead{Outputs} \\
\midrule
\endhead
\midrule
\multicolumn{4}{r}{\small\textit{Tiếp theo trang sau}} \\
\endfoot
\bottomrule
\endlastfoot

\texttt{ListSellerPendingItems} & Lấy inbox mục chờ của seller (đã thanh toán, chưa có order\_id) & \texttt{seller\_id UUID}, pagination & \texttt{[]OrderItem} (dùng partial index) \\
\texttt{ListBuyerPendingItems} & Lấy danh sách mục chờ của buyer & \texttt{account\_id UUID}, pagination & \texttt{[]OrderItem} (dùng partial index) \\
\texttt{InsertOrder} & Tạo đơn hàng khi seller xác nhận items đã thanh toán & \texttt{buyer\_id}, \texttt{seller\_id}, \texttt{transport\_id}, costs, address & \texttt{Order} \\
\texttt{InsertPayment} & Tạo phiên thanh toán tại checkout & \texttt{account\_id}, \texttt{option}, \texttt{amount}, \texttt{date\_expired} & \texttt{Payment} \\
\texttt{ConfirmPaymentByRefID} & Xử lý webhook, cập nhật trạng thái thanh toán & \texttt{ref\_id string}, \texttt{status} & --- \\
\texttt{GetSellerStats} & Aggregate thống kê doanh thu & \texttt{seller\_id}, date\_range & totalRevenue, orderCount, avgValue \\
\end{longtable}
\rowcolors{1}{}{}
\normalsize

Mỗi query ánh xạ 1:1 sang hàm Go do SQLC sinh ra. Ví dụ: \texttt{GetPendingItemsBySeller} tạo ra hàm \texttt{(q *Queries) GetPendingItemsBySeller(ctx, params) ([]OrderItem, error)} trong file \texttt{internal/module/order/db/sqlc/generated\_queries.sql.go}.

% ------------------------------------------------------------------
\subsection{Ràng buộc toàn vẹn dữ liệu (DB constraints)}
\label{subsec:db-integrity}

ShopNexus không dùng TRIGGER. Thay vào đó, toàn vẹn dữ liệu được đảm bảo qua ba cơ chế phối hợp:

\begin{enumerate}[nosep]
    \item \textbf{DB-level constraints} (FK, UNIQUE, enum, partial index) --- nêu trong phần này.
    \item \textbf{Business rules ở tầng biz} --- xem phần~\ref{subsec:db-biz-rules}.
    \item \textbf{Durable execution qua Restate} --- đảm bảo exactly-once cho side effects.
\end{enumerate}

Lý do chọn constraint thay trigger:

\begin{itemize}[nosep]
    \item \textbf{Auditable}: constraint nằm trong migration file, review được qua Pull Request.
    \item \textbf{Transactional}: tự động bảo vệ trong transaction, không race condition.
    \item \textbf{Debug dễ}: lỗi constraint violation rõ ràng, không ẩn sau trigger logic.
    \item \textbf{Tránh lock contention}: trigger có thể gây lock chéo và khó viết test đơn vị.
\end{itemize}

\small
\rowcolors{2}{white}{rowgray}
\begin{longtable}{@{}L{4.2cm}L{2.2cm}L{2.6cm}L{4.2cm}@{}}
\caption{Các constraint và index chính trong schema \texttt{order}}\label{tab:db-constraints-detail}\\
\toprule
\rowcolor{headerblue}
\thead{Ràng buộc} & \thead{Loại} & \thead{Trên bảng} & \thead{Mô tả} \\
\midrule
\endfirsthead
\toprule
\rowcolor{headerblue}
\thead{Ràng buộc} & \thead{Loại} & \thead{Trên bảng} & \thead{Mô tả} \\
\midrule
\endhead
\midrule
\multicolumn{4}{r}{\small\textit{Tiếp theo trang sau}} \\
\endfoot
\bottomrule
\endlastfoot
\texttt{cart\_item\_account\_id\_sku\_id\_key} & UNIQUE & \texttt{cart\_item} & Một tài khoản chỉ có 1 dòng/SKU trong giỏ (idempotent UpdateCart) \\
\texttt{item\_order\_id\_fkey} & FK ON DELETE CASCADE & \texttt{item} $\to$ \texttt{order} & Xóa order xóa hết item liên quan; không tồn tại item mồ côi \\
\texttt{refund\_order\_id\_fkey} & FK ON DELETE NO ACTION & \texttt{refund} $\to$ \texttt{order} & Giữ lại refund record khi order bị xóa --- hỗ trợ audit/legal compliance \\
\texttt{item\_payment\_id\_fkey} & FK ON DELETE SET NULL & \texttt{item} $\to$ \texttt{payment} & Liên kết item với phiên thanh toán tại checkout \\
\texttt{order\_transport\_id\_fkey} & FK ON DELETE SET NULL & \texttt{order} $\to$ \texttt{transport} & Unlink transport khi xóa \\
\texttt{item\_pending\_seller\_idx} & Partial INDEX & \texttt{item (seller\_id)} WHERE \texttt{order\_id IS NULL} & Tối ưu truy vấn inbox mục chờ, giảm kích thước index \\
\texttt{item\_pending\_account\_idx} & Partial INDEX & \texttt{item (account\_id)} WHERE \texttt{order\_id IS NULL} & Tương tự cho buyer inbox \\
\texttt{order.status} enum & TYPE CHECK & payment, order, refund, refund\_dispute & Giới hạn giá trị: Pending, Processing, Success, Cancelled, Failed \\
\texttt{transport\_status} enum & TYPE CHECK & \texttt{transport} & 7 trạng thái lifecycle giao vận \\
\texttt{refund\_method} enum & TYPE CHECK & \texttt{refund} & PickUp hoặc DropOff \\
\end{longtable}
\rowcolors{1}{}{}
\normalsize

% ------------------------------------------------------------------
\subsection{Ràng buộc nghiệp vụ ở tầng Biz (thay cho Trigger)}
\label{subsec:db-biz-rules}

Các ràng buộc không thể biểu diễn qua constraint DB (state transition rules, cross-table invariants, gọi API bên ngoài) được thực thi ở tầng biz (Go handler). Đặc điểm nổi bật: rule chạy bên trong \texttt{restate.Run()} --- durable, idempotent, retry tự động khi có lỗi transient.

Lý do chọn biz rules thay trigger:

\begin{itemize}[nosep]
    \item \textbf{Testable}: unit test rule độc lập với DB state, không cần spin up database.
    \item \textbf{Rõ ràng}: logic trong Go dễ đọc/review hơn PL/pgSQL trigger.
    \item \textbf{Flexibility}: rule phức tạp (gọi API bên ngoài, cross-module) không thể viết trong trigger.
    \item \textbf{Observable}: log, metric, trace đầy đủ qua tầng biz; trigger thường không có.
\end{itemize}

\small
\rowcolors{2}{white}{rowgray}
\begin{longtable}{@{}L{3.5cm}L{3cm}L{3cm}L{3.8cm}@{}}
\caption{Các business rule ở tầng biz (tương đương Trigger)}\label{tab:db-biz-rules}\\
\toprule
\rowcolor{headerblue}
\thead{Rule} & \thead{Thay thế cho} & \thead{File/Module} & \thead{Mô tả} \\
\midrule
\endfirsthead
\toprule
\rowcolor{headerblue}
\thead{Rule} & \thead{Thay thế cho} & \thead{File/Module} & \thead{Mô tả} \\
\midrule
\endhead
\midrule
\multicolumn{4}{r}{\small\textit{Tiếp theo trang sau}} \\
\endfoot
\bottomrule
\endlastfoot

Chuyển trạng thái item & TRIGGER BEFORE UPDATE & Module Order (checkout, xác nhận) & Item không lưu cột status --- trạng thái suy ra: chưa có đơn = chờ xác nhận, đã có đơn = đã xác nhận, có ngày hủy = đã hủy \\
Snapshot giá & TRIGGER BEFORE INSERT & Module Order (checkout) & Khi tạo mục đơn hàng, giá và tên sản phẩm được sao chép từ catalog, bảo vệ khỏi thay đổi giá sau mua \\
Điều kiện hoàn trả & TRIGGER BEFORE INSERT refund & Module Order (hoàn trả) & Chỉ cho phép tạo hoàn trả khi đơn đã thanh toán thành công và không có hoàn trả đang xử lý \\
Thanh toán exactly-once & TRIGGER + retry queue & Module Order (thanh toán) + durable execution & Webhook callback idempotent --- replay không trừ tiền hai lần nhờ journal bền vững \\
Tồn kho nguyên tử & TRIGGER BEFORE UPDATE item & Module Inventory & Đặt chỗ và giải phóng tồn kho thực hiện nguyên tử trong giao dịch durable; thất bại thì rollback toàn bộ \\
Thông báo sau chuyển trạng thái & TRIGGER AFTER UPDATE & Module Order & Gửi thông báo bất đồng bộ tới buyer/seller qua SSE sau mọi thay đổi trạng thái quan trọng \\
Hoàn tiền khi seller duyệt & TRIGGER AFTER UPDATE refund & Module Order (hoàn trả) & Khi seller duyệt hoàn trả: hoàn tiền qua cổng ban đầu, tạo vận đơn ngược, cập nhật trạng thái --- toàn bộ trong một handler durable \\
\end{longtable}
\rowcolors{1}{}{}
\normalsize

% ------------------------------------------------------------------
\subsection{Bảng tham chiếu UC $\to$ Form $\to$ API $\to$ Table}
\label{subsec:db-uc-crossref}

Bảng~\ref{tab:db-uc-crossref} tổng hợp các use case chính, form tương ứng, API endpoint và bảng/query SQLC liên quan --- phục vụ như điểm điều hướng traceability toàn hệ thống. Chi tiết từng module xem tại Bảng~\ref{tab:order-crossref} (module Order) và Bảng~\ref{tab:refund-crossref} (module Refund).

\small
\rowcolors{2}{white}{rowgray}
\begin{longtable}{@{}L{1.5cm}L{3cm}L{3.5cm}L{5.2cm}@{}}
\caption{Tham chiếu UC $\to$ Form $\to$ API $\to$ Bảng/Query chính}\label{tab:db-uc-crossref}\\
\toprule
\rowcolor{headerblue}
\thead{UC} & \thead{Form} & \thead{API} & \thead{Bảng/Query chính} \\
\midrule
\endfirsthead
\toprule
\rowcolor{headerblue}
\thead{UC} & \thead{Form} & \thead{API} & \thead{Bảng/Query chính} \\
\midrule
\endhead
\midrule
\multicolumn{4}{r}{\small\textit{Tiếp theo trang sau}} \\
\endfoot
\bottomrule
\endlastfoot

UC04 & CartPage & UpdateCart, ClearCart & \texttt{order.cart\_item} (UNIQUE sku/account) \\
UC09 & CheckoutPage, SellerInbox & BuyerCheckout, ConfirmSellerPending & \texttt{order.item}, \texttt{order.order} (\texttt{InsertOrder}, \texttt{ReserveInventory}) \\
UC12 & CheckoutPage & BuyerCheckout, ConfirmPayment & \texttt{order.payment} (\texttt{InsertPayment}, \texttt{ConfirmPaymentByRefID}) \\
UC18 & SellerDashboard & GetSellerOrderStats & \texttt{order.order}, \texttt{order.item} (\texttt{GetSellerStats}) \\
UC21 & RefundRequestForm, SellerRefundReview & CreateBuyerRefund, ConfirmSellerRefund & \texttt{order.refund}, \texttt{order.transport} (refund eligibility rule) \\
\end{longtable}
\rowcolors{1}{}{}
\normalsize

% ------------------------------------------------------------------
\subsection{Tiêu chí kiểm thử thiết kế}
\label{subsec:design-test}

Các tiêu chí kiểm thử thiết kế được phân thành bốn nhóm, mỗi nhóm tương ứng với một góc nhìn chất lượng khác nhau: (A)~cấu trúc và chuẩn hoá, (B)~độ tin cậy và tương tranh, (C)~bảo mật và tuân thủ, (D)~hiệu năng và khả năng quan sát. Phương pháp V\&V (Verification \& Validation) được chọn phù hợp với bản chất từng tiêu chí --- từ rà soát ngang hàng (peer review), thanh tra chính thức (Fagan inspection), đến phân tích tự động (static analysis) và mô hình hoá (model checking).

\subsubsection{Phương pháp V\&V áp dụng}
\label{subsubsec:vnv-methods}

\small
\rowcolors{2}{white}{rowgray}
\begin{longtable}{@{}L{3.2cm}L{4.5cm}L{5.5cm}@{}}
\caption{Các phương pháp V\&V trong thiết kế ShopNexus}\label{tab:vnv-methods}\\
\toprule
\rowcolor{headerblue}
\thead{Phương pháp} & \thead{Bản chất} & \thead{Áp dụng trong ShopNexus} \\
\midrule
\endfirsthead
\toprule
\rowcolor{headerblue}
\thead{Phương pháp} & \thead{Bản chất} & \thead{Áp dụng trong ShopNexus} \\
\midrule
\endhead
\midrule
\multicolumn{3}{r}{\small\textit{Tiếp theo trang sau}} \\
\endfoot
\bottomrule
\endlastfoot

Peer Review & Kiểm tra chéo giữa đồng nghiệp & Pull Request bắt buộc trước merge \\
Walkthrough & Tác giả trình bày thiết kế cho team & Design review meeting cho kiến trúc \\
Fagan Inspection & Thanh tra chính thức có moderator / reader / author / recorder với checklist chuẩn & Áp dụng cho critical path: checkout, thanh toán, dispute resolution \\
Static Analysis & Kiểm tra tự động không chạy chương trình & \texttt{golangci-lint}, \texttt{staticcheck}, \texttt{gosec}, chạy trong CI \\
Model Checking & Chứng minh đúng đắn state machine & Transport state machine (đã có unit test \texttt{TestTransportStatusTransitions}) \\
Threat Modeling (STRIDE) & Phân tích đối nghịch 6 loại mối đe doạ & Webhook thanh toán, xác thực JWT \\
FMEA & Failure Modes \& Effects Analysis & Các thành phần critical trong durable execution \\
\end{longtable}
\rowcolors{1}{}{}
\normalsize

\subsubsection{Bảng tiêu chí kiểm thử thiết kế chi tiết}

\small
\rowcolors{2}{white}{rowgray}
\begin{longtable}{@{}L{3.3cm}L{5.3cm}L{4.2cm}L{2.2cm}@{}}
\caption{Tiêu chí kiểm thử thiết kế (15 tiêu chí, 4 nhóm)}\label{tab:design-test}\\
\toprule
\rowcolor{headerblue}
\thead{Tiêu chí} & \thead{Phép thử / Đối tượng} & \thead{Kết quả mong đợi} & \thead{Phương pháp} \\
\midrule
\endfirsthead
\toprule
\rowcolor{headerblue}
\thead{Tiêu chí} & \thead{Phép thử / Đối tượng} & \thead{Kết quả mong đợi} & \thead{Phương pháp} \\
\midrule
\endhead
\midrule
\multicolumn{4}{r}{\small\textit{Tiếp theo trang sau}} \\
\endfoot
\bottomrule
\endlastfoot

\rowcolor{white}\multicolumn{4}{@{}l}{\textit{\textbf{Nhóm A --- Cấu trúc và chuẩn hoá (Static structural review)}}} \\
Chuẩn hoá 3NF & Rà soát ERD: kiểm tra không có phụ thuộc bắc cầu, mỗi bảng có khoá chính rõ ràng & Mỗi cột non-key phụ thuộc trực tiếp vào khoá chính; không dư thừa dữ liệu & Walkthrough \\
Tuân thủ SOLID & Rà soát interface: mỗi module chỉ expose một interface nghiệp vụ, không có interface quá lớn & Single Responsibility, Interface Segregation đạt yêu cầu & Inspection + Peer Review \\
Coupling / Cohesion & Phân tích đồ thị phụ thuộc: đếm afferent/efferent coupling mỗi module & Afferent $\leq$ 5, Efferent $\leq$ 8; không có phụ thuộc vòng & Static Analysis \\
Testability & Walkthrough: mỗi phương thức public có đầu vào/đầu ra rõ ràng, không phụ thuộc trạng thái toàn cục & Viết unit test mà không cần mock liên schema & Walkthrough \\
Modularity & Kiểm tra: không FK xuyên schema, giao tiếp module chỉ qua interface nghiệp vụ & Có thể tách module thành microservice chỉ đổi địa chỉ service & Inspection \\
\addlinespace
\rowcolor{white}\multicolumn{4}{@{}l}{\textit{\textbf{Nhóm B --- Độ tin cậy và tương tranh (Reliability \& concurrency)}}} \\
FMEA --- Failure Modes & Với mỗi thành phần quan trọng (xử lý đơn, cổng thanh toán, tồn kho), liệt kê kịch bản lỗi và biện pháp khắc phục & Mỗi failure mode có retry hoặc compensation rõ ràng & FMEA Table \\
Crash recovery & Kịch bản: crash giữa luồng checkout ngay sau khi đặt chỗ tồn kho; hệ thống replay journal & Không đặt chỗ trùng, không có mục mồ côi; trạng thái cuối nhất quán & Fault Injection + Inspection \\
Idempotency & Rà soát: mọi side effect nằm trong giao dịch durable; webhook kiểm tra mã tham chiếu trùng & Replay cùng event không thay đổi trạng thái; webhook gửi lại không trừ tiền 2 lần & Inspection + Unit Test \\
Race condition \& lock ordering & Phân tích: xác định các truy cập đồng thời (cart, inventory); thứ tự lock (buyer $\to$ seller) & Không deadlock; không lost update; inventory reserve nguyên tử & Walkthrough + Concurrency Test \\
\addlinespace
\rowcolor{white}\multicolumn{4}{@{}l}{\textit{\textbf{Nhóm C --- Bảo mật và tuân thủ (Security \& compliance)}}} \\
STRIDE threat model & Phân tích 6 loại mối đe doạ cho webhook thanh toán, xác thực JWT, dispute submission & Mỗi đe doạ có mitigation (HMAC verify, token expiry, rate limit, audit log) & Threat Modeling \\
Input validation coverage & Rà soát: 100\% endpoint có kiểm tra dữ liệu đầu vào tại middleware & Từ chối dữ liệu không hợp lệ trước khi vào tầng nghiệp vụ & Static Analysis + Inspection \\
AuthN/AuthZ matrix & Checklist: mọi endpoint phi-public yêu cầu JWT; ma trận (endpoint, vai trò, kiểm tra) cho buyer/seller/admin & Không có endpoint bỏ sót; buyer không truy cập đơn của tài khoản khác & Inspection + Pen-test \\
Compliance checklist & Kiểm tra: NĐ 52/2013 (audit trail), Luật BVQLNTD 2023 (14-ngày hoàn trả), PCI-DSS (không lưu card data) & Đạt đủ yêu cầu pháp lý cho TMĐT tại Việt Nam & Peer Review + Legal Review \\
\addlinespace
\rowcolor{white}\multicolumn{4}{@{}l}{\textit{\textbf{Nhóm D --- Hiệu năng và khả năng quan sát (Performance \& observability)}}} \\
Complexity \& query plan & Rà soát big-O từng API; EXPLAIN ANALYZE cho query hot path; kiểm tra partial index được dùng & API read $\leq O(\log n)$; query dùng index, không seq scan trên bảng lớn & Inspection + EXPLAIN \\
Observability readiness & Rà soát: mọi handler quan trọng được đo hiệu năng; lỗi có ngữ cảnh đầy đủ; log có trace ID & 100\% handler quan trọng được giám sát; p99 latency từ dashboard & Inspection + Static Analysis \\
Toàn vẹn dữ liệu & Kịch bản: xoá order $\to$ item CASCADE, refund NO ACTION giữ lại; enum chặn giá trị không hợp lệ & FK rule + enum + partial index đúng thiết kế & Inspection + Integration Test \\

\end{longtable}
\rowcolors{1}{}{}
\normalsize

\subsubsection{Ma trận truy vết yêu cầu --- thiết kế --- kiểm thử (Traceability Matrix)}
\label{subsubsec:traceability}

Ma trận truy vết (traceability matrix) liên kết mỗi yêu cầu (NFR hoặc Use Case) tới thành phần thiết kế hiện thực nó và tới test case kiểm chứng nó. Mục đích: (a)~phát hiện yêu cầu \textit{không được thực hiện} (orphan requirement); (b)~phát hiện thiết kế \textit{không phục vụ yêu cầu nào} (over-engineering); (c)~khi yêu cầu thay đổi, xác định nhanh vùng ảnh hưởng.

\small
\rowcolors{2}{white}{rowgray}
\begin{longtable}{@{}L{1.3cm}L{3.5cm}L{4.2cm}L{3cm}L{2.3cm}@{}}
\caption{Ma trận truy vết: NFR/UC $\to$ Thiết kế $\to$ Kiểm thử $\to$ V\&V}\label{tab:traceability}\\
\toprule
\rowcolor{headerblue}
\thead{ID} & \thead{Yêu cầu} & \thead{Thiết kế hiện thực} & \thead{Test case} & \thead{V\&V} \\
\midrule
\endfirsthead
\toprule
\rowcolor{headerblue}
\thead{ID} & \thead{Yêu cầu} & \thead{Thiết kế hiện thực} & \thead{Test case} & \thead{V\&V} \\
\midrule
\endhead
\midrule
\multicolumn{5}{r}{\small\textit{Tiếp theo trang sau}} \\
\endfoot
\bottomrule
\endlastfoot

\rowcolor{white}\multicolumn{5}{@{}l}{\textit{\textbf{Nghiệp vụ (Business NFR)}}} \\
B02 & Tồn kho chính xác, không overselling & Đặt chỗ tồn kho nguyên tử trong giao dịch durable; partial index cho inbox mục chờ & Test rollback tồn kho + integration test mua đồng thời & Unit + Inspection \\
B04 & Nhất quán chi phí & Công thức tổng tiền thống nhất khi xác nhận đơn + snapshot giá tại checkout & Test công thức + test snapshot giá & Unit + Walkthrough \\
B05 & Phân tích kinh doanh $\leq$ 1s & Truy vấn aggregate trực tiếp trên DB + partial index, không xử lý vòng lặp ở tầng ứng dụng & Benchmark 10K đơn hàng & Performance Test \\
\addlinespace
\rowcolor{white}\multicolumn{5}{@{}l}{\textit{\textbf{Vận hành (Operational NFR)}}} \\
O01 & Xác thực \& phân quyền & Middleware JWT + kiểm tra quyền sở hữu tại tầng nghiệp vụ & Test vi phạm quyền sở hữu + kiểm thử xâm nhập & Inspection + Pen-test \\
O02 & Bảo mật thanh toán & Xác minh chữ ký HMAC webhook, thời hạn phiên thanh toán & Test replay webhook + test phiên hết hạn & Threat Model + Unit \\
O03 & Sẵn sàng \& phục hồi & Cụm Restate 3 node + replay journal + idempotent & Chaos test: dừng node giữa luồng checkout & Fault Injection \\
O06 & Chống lạm dụng endpoint & Rate limiter dựa trên Redis + middleware HTTP & Test fallback bộ nhớ + test cách ly theo IP & Unit + Load Test \\
\addlinespace
\rowcolor{white}\multicolumn{5}{@{}l}{\textit{\textbf{Phát triển (Development NFR)}}} \\
D01 & Durable Microservices & Interface nghiệp vụ + proxy liên module + schema isolation & Rà soát đồ thị phụ thuộc & Static Analysis \\
D03 & Durable Execution & Mọi side effect trong giao dịch durable, gọi liên module qua proxy & Test replay journal + checklist rà soát & Inspection + Unit \\
D06 & Observability & Mọi handler quan trọng đo hiệu năng + dashboard giám sát & Kiểm tra endpoint metrics trả đúng format & Inspection \\
\addlinespace
\rowcolor{white}\multicolumn{5}{@{}l}{\textit{\textbf{Use cases critical}}} \\
UC04 & Đặt hàng từ giỏ hàng & Checkout: tra cứu catalog + đặt chỗ tồn kho + thanh toán + tạo mục chờ & Integration test luồng đầy đủ + test serialization schema & Unit + Integration \\
UC09 & Xác nhận đơn & Xác nhận: tạo vận đơn $\to$ tạo đơn hàng $\to$ gắn mục vào đơn & Test seller khác xác nhận đơn $\to$ bị từ chối & Unit + Authorization Test \\
UC12 & Thanh toán & Checkout + tạo phiên qua cổng + xử lý webhook & E2E: VNPay sandbox + replay webhook & Integration + Pen-test \\
UC18 & Tạo hoàn trả & Kiểm tra điều kiện hoàn trả + partial refund theo item & Test hoàn trả trùng + hoàn trả đồng thời & Unit + Integration \\
UC22 & Mở tranh chấp & Kiểm tra quyền + giới hạn tốc độ 3 lần/phút + thông báo bất đồng bộ & Test giới hạn tốc độ + test phân quyền & Unit + Load Test \\

\end{longtable}
\rowcolors{1}{}{}
\normalsize

\textbf{Phân tích coverage}: 5 NFR critical (B02, O01, O02, O03, D03) và 5 UC critical đều có thiết kế rõ ràng và ít nhất một test case. Các NFR phạm vi rộng (B01 xử lý nhanh, O04 khả năng mở rộng, D02 type safety) được chứng minh qua static analysis và benchmarking thay vì test case riêng.
