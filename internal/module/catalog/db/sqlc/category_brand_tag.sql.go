// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: category_brand_tag.sql

package catalogdb

import (
	"context"

	"github.com/google/uuid"
	null "github.com/guregu/null/v6"
)

const searchCategory = `-- name: SearchCategory :many
SELECT 
    category.id, category.name, category.description, category.parent_id,
    COUNT(*) OVER() as total_count
FROM "catalog"."category" category
WHERE (
   ("id" = ANY($1) OR $1 IS NULL) AND
    (
        "name" ILIKE '%' || $2 || '%' OR
        "description" ILIKE '%' || $2 || '%'  OR
        $2 IS NULL 
    )
)
ORDER BY "id"
LIMIT $4::int
OFFSET $3::int
`

type SearchCategoryParams struct {
	ID     []uuid.UUID `json:"id"`
	Search null.String `json:"search"`
	Offset null.Int32  `json:"offset"`
	Limit  null.Int32  `json:"limit"`
}

type SearchCategoryRow struct {
	CatalogCategory CatalogCategory `json:"catalog_category"`
	TotalCount      int64           `json:"total_count"`
}

func (q *Queries) SearchCategory(ctx context.Context, arg SearchCategoryParams) ([]SearchCategoryRow, error) {
	rows, err := q.db.Query(ctx, searchCategory,
		arg.ID,
		arg.Search,
		arg.Offset,
		arg.Limit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []SearchCategoryRow{}
	for rows.Next() {
		var i SearchCategoryRow
		if err := rows.Scan(
			&i.CatalogCategory.ID,
			&i.CatalogCategory.Name,
			&i.CatalogCategory.Description,
			&i.CatalogCategory.ParentID,
			&i.TotalCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchTag = `-- name: SearchTag :many
SELECT 
    tag.id, tag.description,
    COUNT(*) OVER() as total_count
FROM "catalog"."tag" tag
WHERE (
   ("id" = ANY($1) OR $1 IS NULL) AND
    (
        "id" ILIKE '%' || $2 || '%' OR
        "description" ILIKE '%' || $2 || '%'  OR
        $2 IS NULL 
    )
)
ORDER BY "id"
LIMIT $4::int
OFFSET $3::int
`

type SearchTagParams struct {
	ID     []string    `json:"id"`
	Search null.String `json:"search"`
	Offset null.Int32  `json:"offset"`
	Limit  null.Int32  `json:"limit"`
}

type SearchTagRow struct {
	CatalogTag CatalogTag `json:"catalog_tag"`
	TotalCount int64      `json:"total_count"`
}

func (q *Queries) SearchTag(ctx context.Context, arg SearchTagParams) ([]SearchTagRow, error) {
	rows, err := q.db.Query(ctx, searchTag,
		arg.ID,
		arg.Search,
		arg.Offset,
		arg.Limit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []SearchTagRow{}
	for rows.Next() {
		var i SearchTagRow
		if err := rows.Scan(&i.CatalogTag.ID, &i.CatalogTag.Description, &i.TotalCount); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
