// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: comment_custom.sql

package catalogdb

import (
	"context"

	"github.com/google/uuid"
)

const countRepliesByCommentIDs = `-- name: CountRepliesByCommentIDs :many

SELECT ref_id, COUNT(*) as reply_count
FROM "catalog"."comment"
WHERE ref_type = 'Comment' AND ref_id = ANY($1)
GROUP BY ref_id
`

type CountRepliesByCommentIDsRow struct {
	RefID      uuid.UUID `json:"ref_id"`
	ReplyCount int64     `json:"reply_count"`
}

// Custom comment queries
func (q *Queries) CountRepliesByCommentIDs(ctx context.Context, refIds []uuid.UUID) ([]CountRepliesByCommentIDsRow, error) {
	rows, err := q.db.Query(ctx, countRepliesByCommentIDs, refIds)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []CountRepliesByCommentIDsRow{}
	for rows.Next() {
		var i CountRepliesByCommentIDsRow
		if err := rows.Scan(&i.RefID, &i.ReplyCount); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
