// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: item_custom.sql

package orderdb

import (
	"context"

	"github.com/google/uuid"
)

const cancelItem = `-- name: CancelItem :one
UPDATE "order"."item"
SET "date_cancelled" = CURRENT_TIMESTAMP,
    "cancelled_by_id" = $1
WHERE "id" = $2 AND "date_cancelled" IS NULL
RETURNING id, order_id, account_id, seller_id, sku_id, spu_id, sku_name, address, note, serial_ids, quantity, transport_option, subtotal_amount, total_amount, payment_session_id, date_cancelled, cancelled_by_id, date_created
`

type CancelItemParams struct {
	CancelledByID uuid.NullUUID `json:"cancelled_by_id"`
	ID            int64         `json:"id"`
}

// Marks item cancelled. The compensating refund transaction (negative amount,
// reverses_id pointing to the original payment tx) is inserted separately by biz code.
func (q *Queries) CancelItem(ctx context.Context, arg CancelItemParams) (OrderItem, error) {
	row := q.db.QueryRow(ctx, cancelItem, arg.CancelledByID, arg.ID)
	var i OrderItem
	err := row.Scan(
		&i.ID,
		&i.OrderID,
		&i.AccountID,
		&i.SellerID,
		&i.SkuID,
		&i.SpuID,
		&i.SkuName,
		&i.Address,
		&i.Note,
		&i.SerialIds,
		&i.Quantity,
		&i.TransportOption,
		&i.SubtotalAmount,
		&i.TotalAmount,
		&i.PaymentSessionID,
		&i.DateCancelled,
		&i.CancelledByID,
		&i.DateCreated,
	)
	return i, err
}

const cancelItemsByIDs = `-- name: CancelItemsByIDs :execrows
UPDATE "order"."item"
SET "date_cancelled" = CURRENT_TIMESTAMP
WHERE "id" = ANY($1::BIGINT[])
  AND "order_id" IS NULL
  AND "date_cancelled" IS NULL
`

func (q *Queries) CancelItemsByIDs(ctx context.Context, itemIds []int64) (int64, error) {
	result, err := q.db.Exec(ctx, cancelItemsByIDs, itemIds)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const countBuyerPendingItems = `-- name: CountBuyerPendingItems :one
SELECT COUNT(*) FROM "order"."item"
WHERE "account_id" = $1
  AND "order_id" IS NULL
  AND "date_cancelled" IS NULL
`

func (q *Queries) CountBuyerPendingItems(ctx context.Context, accountID uuid.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countBuyerPendingItems, accountID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countSellerPendingItems = `-- name: CountSellerPendingItems :one
SELECT COUNT(*) FROM "order"."item" i
JOIN "order"."payment_session" ps ON ps."id" = i."payment_session_id"
WHERE i."seller_id" = $1
  AND i."order_id" IS NULL
  AND i."date_cancelled" IS NULL
  AND ps."status" = 'Success'
`

func (q *Queries) CountSellerPendingItems(ctx context.Context, sellerID uuid.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countSellerPendingItems, sellerID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const listBuyerPendingItems = `-- name: ListBuyerPendingItems :many
SELECT id, order_id, account_id, seller_id, sku_id, spu_id, sku_name, address, note, serial_ids, quantity, transport_option, subtotal_amount, total_amount, payment_session_id, date_cancelled, cancelled_by_id, date_created FROM "order"."item"
WHERE "account_id" = $1
  AND "order_id" IS NULL
  AND "date_cancelled" IS NULL
ORDER BY "date_created" DESC
`

func (q *Queries) ListBuyerPendingItems(ctx context.Context, accountID uuid.UUID) ([]OrderItem, error) {
	rows, err := q.db.Query(ctx, listBuyerPendingItems, accountID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []OrderItem{}
	for rows.Next() {
		var i OrderItem
		if err := rows.Scan(
			&i.ID,
			&i.OrderID,
			&i.AccountID,
			&i.SellerID,
			&i.SkuID,
			&i.SpuID,
			&i.SkuName,
			&i.Address,
			&i.Note,
			&i.SerialIds,
			&i.Quantity,
			&i.TransportOption,
			&i.SubtotalAmount,
			&i.TotalAmount,
			&i.PaymentSessionID,
			&i.DateCancelled,
			&i.CancelledByID,
			&i.DateCreated,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listItemsByPaymentSession = `-- name: ListItemsByPaymentSession :many

SELECT id, order_id, account_id, seller_id, sku_id, spu_id, sku_name, address, note, serial_ids, quantity, transport_option, subtotal_amount, total_amount, payment_session_id, date_cancelled, cancelled_by_id, date_created FROM "order"."item" WHERE "payment_session_id" = $1
`

// Custom item queries
// Create/Get/List are auto-generated by pgtempl (CreateDefaultItem / GetItem / ListItem).
func (q *Queries) ListItemsByPaymentSession(ctx context.Context, paymentSessionID int64) ([]OrderItem, error) {
	rows, err := q.db.Query(ctx, listItemsByPaymentSession, paymentSessionID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []OrderItem{}
	for rows.Next() {
		var i OrderItem
		if err := rows.Scan(
			&i.ID,
			&i.OrderID,
			&i.AccountID,
			&i.SellerID,
			&i.SkuID,
			&i.SpuID,
			&i.SkuName,
			&i.Address,
			&i.Note,
			&i.SerialIds,
			&i.Quantity,
			&i.TransportOption,
			&i.SubtotalAmount,
			&i.TotalAmount,
			&i.PaymentSessionID,
			&i.DateCancelled,
			&i.CancelledByID,
			&i.DateCreated,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listPendingPaymentItemsByPaymentSession = `-- name: ListPendingPaymentItemsByPaymentSession :many
SELECT id, order_id, account_id, seller_id, sku_id, spu_id, sku_name, address, note, serial_ids, quantity, transport_option, subtotal_amount, total_amount, payment_session_id, date_cancelled, cancelled_by_id, date_created FROM "order"."item"
WHERE "payment_session_id" = $1
  AND "order_id" IS NULL
  AND "date_cancelled" IS NULL
`

func (q *Queries) ListPendingPaymentItemsByPaymentSession(ctx context.Context, paymentSessionID int64) ([]OrderItem, error) {
	rows, err := q.db.Query(ctx, listPendingPaymentItemsByPaymentSession, paymentSessionID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []OrderItem{}
	for rows.Next() {
		var i OrderItem
		if err := rows.Scan(
			&i.ID,
			&i.OrderID,
			&i.AccountID,
			&i.SellerID,
			&i.SkuID,
			&i.SpuID,
			&i.SkuName,
			&i.Address,
			&i.Note,
			&i.SerialIds,
			&i.Quantity,
			&i.TransportOption,
			&i.SubtotalAmount,
			&i.TotalAmount,
			&i.PaymentSessionID,
			&i.DateCancelled,
			&i.CancelledByID,
			&i.DateCreated,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listSellerPendingItems = `-- name: ListSellerPendingItems :many
SELECT i.id, i.order_id, i.account_id, i.seller_id, i.sku_id, i.spu_id, i.sku_name, i.address, i.note, i.serial_ids, i.quantity, i.transport_option, i.subtotal_amount, i.total_amount, i.payment_session_id, i.date_cancelled, i.cancelled_by_id, i.date_created FROM "order"."item" i
JOIN "order"."payment_session" ps ON ps."id" = i."payment_session_id"
WHERE i."seller_id" = $1
  AND i."order_id" IS NULL
  AND i."date_cancelled" IS NULL
  AND ps."status" = 'Success'
ORDER BY i."date_created" DESC
`

// Only items whose payment session has succeeded — sellers should not see items
// still awaiting buyer payment or whose session has failed.
func (q *Queries) ListSellerPendingItems(ctx context.Context, sellerID uuid.UUID) ([]OrderItem, error) {
	rows, err := q.db.Query(ctx, listSellerPendingItems, sellerID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []OrderItem{}
	for rows.Next() {
		var i OrderItem
		if err := rows.Scan(
			&i.ID,
			&i.OrderID,
			&i.AccountID,
			&i.SellerID,
			&i.SkuID,
			&i.SpuID,
			&i.SkuName,
			&i.Address,
			&i.Note,
			&i.SerialIds,
			&i.Quantity,
			&i.TransportOption,
			&i.SubtotalAmount,
			&i.TotalAmount,
			&i.PaymentSessionID,
			&i.DateCancelled,
			&i.CancelledByID,
			&i.DateCreated,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const setItemsOrderID = `-- name: SetItemsOrderID :exec
UPDATE "order"."item"
SET "order_id" = $1
WHERE "id" = ANY($2::BIGINT[]) AND "order_id" IS NULL
`

type SetItemsOrderIDParams struct {
	OrderID uuid.NullUUID `json:"order_id"`
	ItemIds []int64       `json:"item_ids"`
}

func (q *Queries) SetItemsOrderID(ctx context.Context, arg SetItemsOrderIDParams) error {
	_, err := q.db.Exec(ctx, setItemsOrderID, arg.OrderID, arg.ItemIds)
	return err
}

const sumTotalAmountByOrder = `-- name: SumTotalAmountByOrder :one
SELECT COALESCE(SUM("total_amount"), 0)::BIGINT AS total
FROM "order"."item"
WHERE "order_id" = $1 AND "date_cancelled" IS NULL
`

func (q *Queries) SumTotalAmountByOrder(ctx context.Context, orderID uuid.NullUUID) (int64, error) {
	row := q.db.QueryRow(ctx, sumTotalAmountByOrder, orderID)
	var total int64
	err := row.Scan(&total)
	return total, err
}

const unlinkItemsFromOrder = `-- name: UnlinkItemsFromOrder :exec
UPDATE "order"."item"
SET "order_id" = NULL
WHERE "order_id" = $1
`

func (q *Queries) UnlinkItemsFromOrder(ctx context.Context, orderID uuid.NullUUID) error {
	_, err := q.db.Exec(ctx, unlinkItemsFromOrder, orderID)
	return err
}
