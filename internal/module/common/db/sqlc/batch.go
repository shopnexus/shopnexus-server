// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: batch.go

package commondb

import (
	"context"
	"encoding/json"
	"errors"
	"time"

	"github.com/google/uuid"
	null "github.com/guregu/null/v6"
	"github.com/jackc/pgx/v5"
)

var (
	ErrBatchAlreadyClosed = errors.New("batch already closed")
)

const createBatchResource = `-- name: CreateBatchResource :batchone
INSERT INTO "common"."resource" ("id", "uploaded_by", "provider", "object_key", "mime", "size", "metadata", "checksum", "created_at")
VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
RETURNING id, uploaded_by, provider, object_key, mime, size, metadata, checksum, created_at
`

type CreateBatchResourceBatchResults struct {
	br     pgx.BatchResults
	tot    int
	closed bool
}

type CreateBatchResourceParams struct {
	ID         uuid.UUID       `json:"id"`
	UploadedBy uuid.NullUUID   `json:"uploaded_by"`
	Provider   string          `json:"provider"`
	ObjectKey  string          `json:"object_key"`
	Mime       string          `json:"mime"`
	Size       int64           `json:"size"`
	Metadata   json.RawMessage `json:"metadata"`
	Checksum   null.String     `json:"checksum"`
	CreatedAt  time.Time       `json:"created_at"`
}

func (q *Queries) CreateBatchResource(ctx context.Context, arg []CreateBatchResourceParams) *CreateBatchResourceBatchResults {
	batch := &pgx.Batch{}
	for _, a := range arg {
		vals := []interface{}{
			a.ID,
			a.UploadedBy,
			a.Provider,
			a.ObjectKey,
			a.Mime,
			a.Size,
			a.Metadata,
			a.Checksum,
			a.CreatedAt,
		}
		batch.Queue(createBatchResource, vals...)
	}
	br := q.db.SendBatch(ctx, batch)
	return &CreateBatchResourceBatchResults{br, len(arg), false}
}

func (b *CreateBatchResourceBatchResults) QueryRow(f func(int, CommonResource, error)) {
	defer b.br.Close()
	for t := 0; t < b.tot; t++ {
		var i CommonResource
		if b.closed {
			if f != nil {
				f(t, i, ErrBatchAlreadyClosed)
			}
			continue
		}
		row := b.br.QueryRow()
		err := row.Scan(
			&i.ID,
			&i.UploadedBy,
			&i.Provider,
			&i.ObjectKey,
			&i.Mime,
			&i.Size,
			&i.Metadata,
			&i.Checksum,
			&i.CreatedAt,
		)
		if f != nil {
			f(t, i, err)
		}
	}
}

func (b *CreateBatchResourceBatchResults) Close() error {
	b.closed = true
	return b.br.Close()
}

const createBatchResourceReference = `-- name: CreateBatchResourceReference :batchone
INSERT INTO "common"."resource_reference" ("rs_id", "ref_type", "ref_id", "order")
VALUES ($1, $2, $3, $4)
RETURNING id, rs_id, ref_type, ref_id, "order"
`

type CreateBatchResourceReferenceBatchResults struct {
	br     pgx.BatchResults
	tot    int
	closed bool
}

type CreateBatchResourceReferenceParams struct {
	RsID    uuid.UUID             `json:"rs_id"`
	RefType CommonResourceRefType `json:"ref_type"`
	RefID   uuid.UUID             `json:"ref_id"`
	Order   int32                 `json:"order"`
}

func (q *Queries) CreateBatchResourceReference(ctx context.Context, arg []CreateBatchResourceReferenceParams) *CreateBatchResourceReferenceBatchResults {
	batch := &pgx.Batch{}
	for _, a := range arg {
		vals := []interface{}{
			a.RsID,
			a.RefType,
			a.RefID,
			a.Order,
		}
		batch.Queue(createBatchResourceReference, vals...)
	}
	br := q.db.SendBatch(ctx, batch)
	return &CreateBatchResourceReferenceBatchResults{br, len(arg), false}
}

func (b *CreateBatchResourceReferenceBatchResults) QueryRow(f func(int, CommonResourceReference, error)) {
	defer b.br.Close()
	for t := 0; t < b.tot; t++ {
		var i CommonResourceReference
		if b.closed {
			if f != nil {
				f(t, i, ErrBatchAlreadyClosed)
			}
			continue
		}
		row := b.br.QueryRow()
		err := row.Scan(
			&i.ID,
			&i.RsID,
			&i.RefType,
			&i.RefID,
			&i.Order,
		)
		if f != nil {
			f(t, i, err)
		}
	}
}

func (b *CreateBatchResourceReferenceBatchResults) Close() error {
	b.closed = true
	return b.br.Close()
}

const createBatchServiceOption = `-- name: CreateBatchServiceOption :batchone
INSERT INTO "common"."service_option" ("id", "category", "name", "description", "provider", "method", "is_active", "order")
VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
RETURNING id, category, name, description, provider, method, is_active, "order"
`

type CreateBatchServiceOptionBatchResults struct {
	br     pgx.BatchResults
	tot    int
	closed bool
}

type CreateBatchServiceOptionParams struct {
	ID          string `json:"id"`
	Category    string `json:"category"`
	Name        string `json:"name"`
	Description string `json:"description"`
	Provider    string `json:"provider"`
	Method      string `json:"method"`
	IsActive    bool   `json:"is_active"`
	Order       int32  `json:"order"`
}

func (q *Queries) CreateBatchServiceOption(ctx context.Context, arg []CreateBatchServiceOptionParams) *CreateBatchServiceOptionBatchResults {
	batch := &pgx.Batch{}
	for _, a := range arg {
		vals := []interface{}{
			a.ID,
			a.Category,
			a.Name,
			a.Description,
			a.Provider,
			a.Method,
			a.IsActive,
			a.Order,
		}
		batch.Queue(createBatchServiceOption, vals...)
	}
	br := q.db.SendBatch(ctx, batch)
	return &CreateBatchServiceOptionBatchResults{br, len(arg), false}
}

func (b *CreateBatchServiceOptionBatchResults) QueryRow(f func(int, CommonServiceOption, error)) {
	defer b.br.Close()
	for t := 0; t < b.tot; t++ {
		var i CommonServiceOption
		if b.closed {
			if f != nil {
				f(t, i, ErrBatchAlreadyClosed)
			}
			continue
		}
		row := b.br.QueryRow()
		err := row.Scan(
			&i.ID,
			&i.Category,
			&i.Name,
			&i.Description,
			&i.Provider,
			&i.Method,
			&i.IsActive,
			&i.Order,
		)
		if f != nil {
			f(t, i, err)
		}
	}
}

func (b *CreateBatchServiceOptionBatchResults) Close() error {
	b.closed = true
	return b.br.Close()
}
