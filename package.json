{
  "devDependencies": {
    "prisma": "^6.15.0",
    "prisma-dbml-generator": "^0.12.0"
  },
  "dependencies": {
    "@faker-js/faker": "^10.0.0",
    "@prisma/client": "^6.15.0",
    "@sindresorhus/slugify": "^3.0.0",
    "uuid": "^13.0.0"
  },
  "scripts": {
    "sqlc": "npx prisma migrate diff --from-empty --to-schema-datamodel prisma --script > prisma/migrations/0_init/migration.sql && .\\tool.exe -schema prisma/migrations/0_init/migration.sql -single-file && sqlc generate"
  },
  "prisma": {
    "schema": "./prisma"
  }
}